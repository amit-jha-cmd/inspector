library inspector;

import 'package:flutter/material.dart';

class InspectorApp extends StatelessWidget {
  const InspectorApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Inspecor')),
    );
  }
}
